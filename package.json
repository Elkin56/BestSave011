{
  "name": "bestsave-trial",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BestSave — бесплатный триал: архивация Telegram-чатов через бота + Mini App",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "setup-webhook": "node scripts/set-webhook.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
