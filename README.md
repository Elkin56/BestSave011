# BestSave — пробная версия (Vercel)

Бесплатный триал: бот-архиватор Telegram-чатов + Mini App для просмотра архива.
Пользователь добавляет бота в свои группы/каналы, назначает администратором —
бот сохраняет сообщения, ловит правки и хранит копии удалённого.

---

## ⚠️ Что этот продукт МОЖЕТ и чего НЕ может

Это не маркетинг, а границы платформы Telegram. Понимание сэкономит вам недели.

**Может (легально, без риска бана):**
- Архивировать сообщения в группах/каналах, где бот — **администратор**
- Ловить **редактирования** (`edited_message` боту приходит) и хранить версию «до»
- Хранить копии, чтобы показать удалённое **позже**
- Сохранять медиа (фото, видео, голосовые, файлы)

**Не может (ограничение Telegram, обойти нельзя без нарушения ToS и закона):**
- Видеть **личные 1-на-1 чаты** пользователя с другими людьми.
  Bot API отдаёт боту только то, что адресовано самому боту или отправлено
  в чат с ботом. Чужую личную переписку бот не видит — и не должен.
- Получать апдейт **в момент удаления**. У ботов такого события нет.
  Поэтому мы не «детектируем удаление», а **храним то, что видели раньше**.

Обойти это можно только userbot-ом (вход в аккаунт по номеру телефона) — а это
прямое нарушение Telegram ToS (бан аккаунта), передача чужого аккаунта третьей
стороне и, в большинстве юрисдикций, незаконный перехват чужих сообщений.
Здесь такого нет намеренно.

---

## Структура

```
api/
  bot.js              вебхук бота: архивация, правки, привязка чатов
  health.js           проверка + разовая инициализация схемы БД
  chats.js            GET список чатов пользователя (нужен initData)
  messages.js         GET сообщения чата (initData + проверка доступа)
  bot-info.js         GET username бота (из getMe) для ссылки добавления в чат
lib/
  auth.js             валидация Telegram initData (HMAC-SHA256, TTL)
  db.js               Postgres: схема + запросы
  invite.js           реферальные ссылки и шаринг (условиями доступа больше не являются)
  digest.js           режим доставки: когда слать, что копить, как собрать сводку
  media-info.js       разбор вложений (тип, file_id, отпечаток) — общий для всех
public/
  index.html          оболочка Mini App
  app.js              фронтенд, ходит в /api/* с initData
  privacy.html        политика конфиденциальности
  terms.html          пользовательское соглашение
  legal.css           оформление обоих документов
  cat.jpg             маскот
scripts/
  set-webhook.js      разовая привязка вебхука
test/
  auth.test.js        9 тестов валидации initData
  invite.test.js      реферальные ссылки + проверка, что гейт не вернулся
  digest.test.js      сроки, режимы, склейка сводки
vercel.json
package.json
```

---

## Если Vercel пишет «unmatched-function-pattern» или не видит api

Это значит, что **папка `api` не загрузилась в репозиторий** (частая беда при
загрузке через веб-GitHub — вложенные папки теряются). Проверьте на GitHub, что
в репозитории есть все четыре файла: `api/bot.js`, `api/health.js`,
`api/chats.js`, `api/messages.js`. Если их нет — загрузите заново.

Надёжнее не тащить папки мышкой в браузере, а через командную строку:
```bash
cd bestsave-trial
git init && git add -A && git commit -m "bestsave trial"
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/ВАШ_РЕПО.git
git push -u origin main
```
`git add -A` берёт все файлы и папки рекурсивно — ничего не потеряется.

В этой версии структура `api/` **плоская** (без вложенных папок) именно затем,
чтобы веб-загрузка на GitHub её не теряла.

---

## Развёртывание — по шагам

### 1. Создать бота
В Telegram напишите [@BotFather](https://t.me/BotFather): `/newbot`.
Сохраните **BOT_TOKEN**. Затем **обязательно** выключите Privacy Mode, иначе
бот не увидит сообщения в группах даже будучи админом:
`/setprivacy` → выберите бота → **Disable**.


### 2. Завести базу Postgres
Любой управляемый Postgres. Проще всего — [Neon](https://neon.tech) (free tier)
или Vercel Postgres. Скопируйте строку подключения (`DATABASE_URL`).

### 3. Задеплоить на Vercel
```bash
npm i -g vercel
cd bestsave-trial
vercel            # первый деплой (preview)
vercel --prod     # прод
```
Либо загрузите папку в Git и импортируйте репозиторий в дашборде Vercel.

### 4. Переменные окружения (Vercel → Settings → Environment Variables)
| Переменная | Значение |
|---|---|
| `BOT_TOKEN` | токен от BotFather |
| `DATABASE_URL` | строка подключения Postgres |
| `WEBHOOK_SECRET` | любая случайная строка (защита вебхука) |
| `PUBLIC_URL` | публичный адрес проекта — для ссылок на документы в `/privacy` |
| — | `WEBHOOK_SECRET` используется и для ручного вызова `/api/cron?secret=…` |
| `ADMIN_TG_IDS` | ваш tg id (доступ к панели владельца) |
| `COMMUNITY_CHAT` | `@юзернейм` канала сообщества, по умолчанию `@bestsavee` |
| `COMMUNITY_TITLE` | название канала в интерфейсе, по умолчанию `BestSave Community` |
| `COMMUNITY_URL` | только для приватного канала: инвайт-ссылка |

После добавления сделайте **Redeploy**, чтобы переменные подхватились.

### 5. Инициализировать схему
Откройте `https://ваш-проект.vercel.app/api/health` — таблицы создадутся,
в ответе увидите `"schema": true`.

### 6. Привязать вебхук
```bash
BOT_TOKEN=... \
WEBHOOK_URL=https://ваш-проект.vercel.app/api/bot \
WEBHOOK_SECRET=...тот же секрет... \
node scripts/set-webhook.js
```
Ответ `{"ok": true}` — вебхук стоит.

### 7. Подключить Mini App к боту
В BotFather: `/newapp` (или `/myapps`) → выберите бота → укажите URL
`https://ваш-проект.vercel.app`. Появится кнопка запуска приложения.

### 8. Проверить
1. Напишите боту `/start` — архив открыт сразу, никаких условий
2. Создайте тестовую группу, добавьте бота, назначьте админом
3. Бот пришлёт «Готово, архив начат»
4. Напишите пару сообщений, одно отредактируйте
5. Откройте Mini App → чат появится, правка сохранится

---

## Локальный запуск тестов
```bash
npm test          # 9 тестов валидации initData
```

---

## Приглашения

Условий доступа нет. Архив открывается сразу после `/start` — ни подписки на
канал, ни приглашённых друзей не требуется.

Осталась только команда `/invite`: личная ссылка `t.me/<бот>?start=ref<id>`
и счётчик тех, кто по ней пришёл. Поделиться ею — дело добровольное, ни на
что в приложении она не влияет.

`referral.invited_tg_id` — первичный ключ: один человек засчитывается ровно
одному пригласившему и ровно один раз. Реферал считается только за нового
пользователя (проверка `userExists` идёт **до** `upsertUser`), самоприглашение
отсекается.

Что изменилось при снятии гейта:

* В `api/[...route].js` больше нет набора `GATED` и общей проверки перед
  обработчиками — маршруты отвечают сразу, авторизации по `initData` довольно.
* Маршрут `/api/gate` **оставлен** и всегда отвечает `{ passed: true }`.
  Mini App кэшируется у пользователя на устройстве, и клиент прошлой версии
  первым делом дёргает этот адрес: ответь мы 404 — приложение упало бы на
  старте. Через несколько релизов маршрут можно убрать.
* Колонка `app_user.gate_passed_at` в старых базах остаётся, но не читается.
  Схема её больше не создаёт. Дропать не стали: молча удалять данные нельзя.
* `lib/gate.js` переименован в `lib/invite.js` — от него осталась работа
  со ссылками. Проверка подписки (`getChatMember`) и сборка состояния гейта
  удалены, поэтому админом канала бот быть больше не обязан.

## Режим доставки уведомлений (дайджест)

Уведомления об удалении, правках и повторных медиа доставляются в одном из
четырёх режимов — «Настройки» → «Как доставлять»:

| Режим | Поведение |
|---|---|
| `instant` | каждое событие отдельным сообщением (по умолчанию) |
| `hourly` | накопленное за час — одной сводкой |
| `daily` | одна сводка в 9:00 по местному времени пользователя |
| `silent` | бот не пишет; события видны только в приложении |

**Побочный выигрыш — тихие часы.** Раньше ночное событие просто не
отправлялось: проверка `isQuietNow` стояла в каждом из трёх мест и молча
съедала уведомление. Теперь ночью событие встаёт в очередь и приходит утром.
Это была потеря данных, а не настройка.

### Как устроено

* `lib/digest.js` — чистая логика: когда наступил срок (`isDigestDue`), копить
  или слать (`shouldQueue`), как собрать текст (`buildDigest`). Ни базы, ни
  сети, поэтому всё под тестами без окружения.
* `pending_notice` — очередь. Хранит готовый текст и `target_chat_id`: к моменту
  отправки бизнес-подключения может уже не быть, и искать адресата поздно.
* Единая точка отправки `notify()` в `api/bot.js`. Все три уведомления идут
  через неё; прямых `sendMessage` в обход больше нет — за этим следит тест.

### Что будит очередь

Два независимых механизма, потому что одного не хватает:

1. **Попутный сброс на вебхуке** (`limit: 5`) — основной. Вебхук вызывается
   постоянно, пока ботом пользуются, и сбрасывает очереди **всех** владельцев,
   у кого подошёл срок, а не только того, чей апдейт пришёл. Лимит маленький:
   Telegram ждёт быстрый ответ, иначе доставит апдейт повторно.
2. **Cron** `/api/cron` — страховка на случай, когда бот простаивает и вебхук
   молчит.

⚠️ **Расписание cron — раз в сутки (`0 6 * * *`), и менять его на более частое
нельзя без платного тарифа.** Бесплатный тариф Vercel отвергает любое
расписание чаще суточного с ошибкой *«Hobby accounts are limited to daily Cron
Jobs»*, и падает **весь деплой**, а не только cron. На Pro можно поставить
`0 * * * *`. За этим следит тест `vercel-config.test.js` — он не даст
отправить в деплой расписание, которое Vercel отвергнет.

Из-за суточного cron дневная сводка **не может ловить ровно 9:00** у каждого
пользователя. Поэтому срок проверяется не по часу, а по моменту: отдаём всё,
что накопилось **до последних девяти утра по местному времени**
(`lastDailyMoment`). Сводка уходит при первой же возможности после срока —
попутным сбросом или ближайшим cron, — а не пропадает до завтра. События,
пришедшие после девяти, ждут завтрашней сводки.

Забор из очереди — `DELETE … RETURNING`: cron и вебхук могут сработать
одновременно, и только атомарный забор гарантирует одну сводку вместо двух.
Проверено тестом на гонку.

Если Telegram отказал (бот заблокирован, лимит), события **возвращаются**
в очередь. От вечного накопления защищает предохранитель `STALE_HOURS = 25`:
просроченное уходит независимо от режима, тишины и часа — сбой cron не должен
превращаться в молчание навсегда.

## Правовые документы

Два статичных документа лежат в `public/` и отдаются Vercel по адресам
`/privacy.html` и `/terms.html`. Ссылки на них есть в приложении
(«Настройки» → «Конфиденциальность» → «Документы») и в команде `/privacy`.

**Перед публикацией заполните места, отмеченные жёлтым пунктиром:** реквизиты
оператора, e-mail для обращений, провайдер базы, страна применимого права.
Поиском по `[укажите` найдёте все.

Отдельно стоит указать адрес политики в BotFather: *Bot Settings → Privacy
Policy* — Telegram показывает её в меню мини-приложения.

Три места, которые заслуживают внимания юриста до коммерческого запуска:

* **Роли операторов.** В отношении данных собеседников оператором выступает
  пользователь, а сервис — обработчиком по его поручению (раздел 4 политики,
  пункт 5.3 соглашения). Это ближе к реальности, чем объявить оператором
  сервис, но конструкция зависит от юрисдикции.
* **Трансграничная передача.** Vercel и управляемый Postgres расположены за
  пределами РФ. Для данных граждан РФ действует требование локализации
  (ч. 5 ст. 18 152-ФЗ).
* **Ограничение ответственности** (раздел 9 соглашения) — норма, которую
  потребительское право сужает сильнее всего.

### Об исчезающих сообщениях

Раздел 7 соглашения фиксирует то, что делает сам код: одноразовые
(«просмотр один раз») и самоуничтожающиеся по таймеру сообщения **не
сохраняются и не запрашиваются**. Telegram не передаёт такой контент ботам, и
обходить это ограничение сервис не пытается — ни через Bot API, ни через
userbot, ни через автоматизацию поверх аккаунта. Попытки сделать это
запрещены пунктом 6 и влекут блокировку.

Причина не только техническая. Отправитель одноразового медиа совершил явное
действие: выбрал, чтобы материал не сохранялся. Согласие пользователя с
соглашением волю другого человека не отменяет — и оговорка в документе её
тоже не заменит.

## Модель безопасности

- **Каждый** `/api/*`-запрос несёт `initData` в заголовке `Authorization: tma <...>`.
  Сервер проверяет HMAC-подпись **и возраст** (не старше суток, не из будущего).
  Без валидной подписи — 401.
- `/api/messages` дополнительно проверяет, что запрошенный чат **привязан именно
  этим пользователем** — иначе по chatId можно было бы читать чужой архив (403).
- Вебхук бота защищён `secret_token`: Telegram шлёт его в заголовке, чужой POST
  на `/api/bot` отклоняется.
- Заголовки безопасности (`X-Content-Type-Options`, `Referrer-Policy`) в `vercel.json`.

---

## Что дальше (для платной версии)
- Пагинация и полнотекстовый поиск по архиву
- AI-анализ (в основном проекте уже есть — вынести в отдельный сервис,
  serverless не держит долгие задачи)
- Экспорт CSV/JSON (логика в основном проекте, `export/csv.ts`)
- Уведомления о правках/удалениях (бот уже видит события — добавить push)

---

## Известные ограничения триала
- Serverless-функции живут секунды: тяжёлый бэкфилл истории здесь неуместен,
  бот архивирует «с момента подключения», а не задним числом.
- `ensureSchema()` вызывается на месте (bootstrap). В проде — заменить на миграции.
- Медиа сохраняются как факт (тип + метаданные); сами файлы в S3 — задача платной версии.
